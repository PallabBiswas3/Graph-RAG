{
  "name": "graph-rag-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for Graph-RAG client and server applications",
  "main": "index.js",
  "private": true,
  "workspaces": [
    "./client",
    "./server"
  ],
  "scripts": {
    "client": "npm run dev --workspace=client",
    "server": "npm run dev --workspace=server",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "start": "npm run serve --workspace=server && npm run preview --workspace=client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@tailwindcss/postcss": "^4.1.18"
  }
}
